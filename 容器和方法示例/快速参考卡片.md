# 快速参考卡片（口袋版）

打印这个文件，放在手边快速查阅！

---

## C++ 容器速查（O(?) 性能）

### 顺序容器
```
vector     [O(1)访]  O(1)尾加  O(n)中插   随机访问最快 常用
deque      [O(1)访]  O(1)双端  O(n)中插   两端灵活
```

### 集合容器
```
unordered_map  {O(1)avg}  哈希表   快速查询   ⚠️自动插入陷阱
map            {O(log)}   有序     范围查询   稳定有序

unordered_set  [O(1)avg]  去重查询  平均快速   最好
set            [O(log)]   有序去重  范围查询

string         O(1)访     += 追加   find返回npos
```

### 高级容器
```
stack          push/pop O(1)      无erase()
queue          push/pop O(1)      无erase()
priority_queue push/pop O(log n)  无erase() ⚠️小根堆
```

---

## Java 容器速查

### 常用列表
```
ArrayList      [O(1)访]  O(1)尾加  O(n)中插   最常用
LinkedList     [O(n)访]  O(1)双端  O(1)中插   罕见，用ArrayDeque
```

### 集合容器
```
HashMap        {O(1)avg}  getOrDefault()安全
TreeMap        {O(log)}   firstKey/lastKey
HashSet        [O(1)avg]  add返回true/false
TreeSet        [O(log)]   有序去重
```

### 栈和队列
```
ArrayDeque     推荐！比Stack/Queue快
PriorityQueue  默认小根堆,(a,b)->b-a 大根堆
```

### 字符串
```
String         ❌频繁+= 低效，用StringBuilder
StringBuilder  ✅ append() 链式调用
```

---

## 8 大常见陷阱速记

### 1️⃣ vector 迭代删除
```cpp
// ❌
for (it = v.begin(); it != v.end(); ++it) { v.erase(it); }

// ✅
for (it = v.begin(); it != v.end(); ) {
    if (/*删什么*/) { it = v.erase(it); }
    else { ++it; }
}
```

### 2️⃣ map[key] 自动插入
```cpp
// ❌
if (mp[key] > 0) { }  // 若不存在会创建

// ✅
if (mp.count(key) && mp[key] > 0) { }
```

### 3️⃣ string::npos
```cpp
// ❌
if (s.find(x) == -1) { }  // 类型不对

// ✅
if (s.find(x) != string::npos) { }
```

### 4️⃣ unique() 只移动
```cpp
// ❌
unique(v.begin(), v.end());  // v 大小不变

// ✅
auto it = unique(v.begin(), v.end());
v.erase(it, v.end());
```

### 5️⃣ priority_queue 无 erase
```cpp
// ❌
pq.erase(x);  // 编译错误！

// ✅
// 懒删除：用 set 记录已删除，出队时检查
unordered_set<int> deleted;
```

### 6️⃣ Java ArrayList 遍历删除
```java
// ❌
for (int x : list) { list.remove(x); }  // 异常！

// ✅
Iterator<Integer> it = list.iterator();
while (it.hasNext()) {
    if (/*删什么*/) it.remove();
    else it.next();
}
```

### 7️⃣ Java HashMap.get() 返回 null
```java
// ❌
int count = map.get(key);
if (count > 0) { }  // NullPointerException!

// ✅
int count = map.getOrDefault(key, 0);
```

### 8️⃣ Java String 频繁修改
```java
// ❌
String s = "";
for (int i = 0; i < 10000; i++) { s += i; }  // O(n²)

// ✅
StringBuilder sb = new StringBuilder();
for (int i = 0; i < 10000; i++) { sb.append(i); }
```

---

## 容器选择速查表

| 我需要... | C++ | Java |
|---------|-----|------|
| 任意访问 | vector | ArrayList |
| 两端操作 | deque | **ArrayDeque** |
| 快速查询 | unordered_set | HashSet |
| 有序+范围 | map | TreeMap |
| 统计频率 | unordered_map | HashMap |
| 栈/括号 | stack | **ArrayDeque(栈)** |
| BFS/队列 | queue | **ArrayDeque(队列)** |
| 优先级 | priority_queue | PriorityQueue |

---

## 复杂度对比

### 搜索：O(?) 
```
数组搜索    O(n)     线性
二分搜索    O(log n) 前提排序
哈希表      O(1)avg  最好但有碰撞
二叉搜索树  O(log n) 有序
```

### 排序：O(?) 
```
快排        O(n log n)avg   C++用
归并        O(n log n)稳定  Java对象用
堆排        O(n log n)恒定  
插入        O(n²)     小数据快
```

### 容器操作：O(?)
```
vector      [O(1)]  O(n)  O(n)    插/删/查
链表        [O(n)]  O(1)* O(n)    *需要指针
哈希表      [--]    O(1)  O(1)avg
二叉树      [O(log)]O(log)O(log)
```

---

## 性能陷阱排行

> 最容易导致 TLE 的容器错误

**🥇 第1名**：选错容器
- 用了 O(n²) 当需要 O(n log n)
- 用 vector 做频繁中间插删

**🥈 第2名**：string 频繁拼接
- Java 中 String + 一千次 = O(n²)
- 开销：每次复制整个字符串

**🥉 第3名**：priority_queue 懒加载
- 每次出队检查太多无用元素
- 解决：用标记提前过滤

**4️⃣ 第4名**：map 嵌套过深
- map<int, map<int, vector>> 查询 O(log² n + m)
- 改为 map<pair<int,int>, vector>

**5️⃣ 第5名**：未预留容量
- vector 频繁自动扩容：O(1)avg 退化
- 解决：`v.reserve(n)` 预分配

---

## 输出格式速查

### C++
```cpp
cout << v[0] << "\n";                      // int
cout << s << "\n";                         // string
for (auto x : v) cout << x << " ";         // 遍历
cout << "结果: " << ans << endl;            // 调试输出
```

### Java
```java
System.out.println(list);                  // List 直接打印
System.out.println(Arrays.toString(arr));  // 数组转字符串
System.out.println(map);                   // Map 直接打印
```

---

## 调试技巧速记

### C++ 调试
```cpp
assert(i >= 0 && i < n);                   // 断言检查
cerr << "DEBUG: x=" << x << "\n";          // 输到 stderr
v.erase(remove(v.begin(), v.end(), x), v.end());  // 删所有x
```

### Java 调试
```java
assert i >= 0 && i < n;                    // 断言（需 -ea 参数）
System.err.println("DEBUG: x=" + x);       // 输到 stderr
list.removeAll(Collections.singleton(x));  // 删所有x
```

---

## 最常用 API 排行榜

### C++ TOP 10
1. `v.push_back(x)` - 添加元素
2. `mp[key]` - 访问/插入
3. `mp.count(key)` - 判断存在
4. `for (auto x : v)` - 遍历
5. `sort(v.begin(), v.end())` - 排序
6. `lower_bound(v.begin(), v.end(), x)` - 二分
7. `st.push(x)` / `st.pop()` - 栈操作
8. `q.push(x)` / `q.pop()` - 队列操作
9. `pq.push(x)` / `pq.top()` - 堆操作
10. `s.find(sub)` - 字符串查找

### Java TOP 10
1. `list.add(x)` - 添加
2. `map.get(key)` - 访问
3. `map.put(key, val)` - 插入
4. `for (int x : list)` - 遍历
5. `Collections.sort(list)` - 排序
6. `Collections.binarySearch(list, x)` - 二分
7. `stack.push()` - 栈操作（用 ArrayDeque）
8. `queue.offer()` - 队列操作（用 ArrayDeque）
9. `pq.poll()` - 堆操作
10. `s.contains(str)` - 字符串查找

---

## 秒速判断正确性

编码前问自己：
- [ ] 空输入能处理吗？
- [ ] 边界条件（n=1, 全相同）呢？
- [ ] 整数溢出了没？
- [ ] 复杂度符合时限吗？
- [ ] 选对容器了吗？
- [ ] 迭代时会修改容器吗？
- [ ] 有 off-by-one 错误吗？

✅ 全是 YES → 提交！

---

## 记忆技巧

### "O Big" 口诀
```
一次 O(1)     数组访问秒级
对数 O(log)   二分搜索百万级
线性 O(n)     逐个扫全部
二次 O(n²)    嵌套循环百万×百万
指数 O(2ⁿ)    指数爆炸绝不选
```

### "容器套路"
```
见着排序想归并，见着查询想哈表
见着区间想二分，见着最值想堆排
见着括号想栈用，见着路径想队列
见着优先想堆顶，见着去重想集合
```

### "删除套路"
```
删数组？用 erase
删 map？中间无法删
删优先队列？标记删除
```

---

## 文件对应速查

| 查什么 | 去哪里 |
|-------|--------|
| vector 怎么用 | 容器示例/vector_usage.cpp |
| map/set 怎么用 | 容器示例/string_map_set_usage.cpp |
| Java 全容器 | 容器示例/JavaContainersUsage.java |
| API 列表 | 根目录 C++常用容器.md |
| 错解改法 | 容器示例/最佳实践与常见陷阱.md |
| 学习路径 | 容器示例/README.md |

---

打印这页，贴在显示器旁，刷题效率翻倍！🚀

