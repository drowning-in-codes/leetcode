# C++ 开发者的 Java 面试冲刺清单

> 一周速成！面试前必背清单。从代码实践到模拟面试，循序渐进。

---

## 🎯 面试前最后一周计划

### Day 1-2：API 速记（2天）
- [ ] 背 ArrayList / HashMap / HashSet 的 5 个常用方法
- [ ] 理解 get/set 的区别（不能用 []）
- [ ] 代码实战：10 道 Easy 题（全部用 HashMap 或 ArrayList）

### Day 3：字符串与排序（1天）
- [ ] String 不可变性 + StringBuilder 使用
- [ ] Collections.sort() 和 Lambda 比较器
- [ ] 代码实战：5 道字符串题 + 5 道排序题

### Day 4：高级数据结构（1天）
- [ ] PriorityQueue（Top K 题型）
- [ ] Deque 作为栈和队列
- [ ] 代码实战：5 道堆/栈/队列题

### Day 5：算法模板（1天）
- [ ] 两指针、滑动窗口、二分、BFS
- [ ] 代码实战：混合难度各 3 题

### Day 6-7：模拟面试（2天）
- [ ] 完整模拟 2 场：各 3 道题（Easy + Medium）
- [ ] 包括讲解（英文或中文）
- [ ] 检查是否有 API 错误、边界处理

---

## ⭐ 必背 API 速查表

### 绝对必背（出现频率 > 90%）

```java
// ArrayList
List<Integer> list = new ArrayList<>();
list.add(x);              // 添加
list.get(i);              // 获取
list.set(i, x);           // 修改
list.remove(i);           // 删除
list.size();              // 大小
list.isEmpty();           // 是否为空
for (int x : list) {}     // 遍历

// HashMap
Map<K, V> map = new HashMap<>();
map.put(k, v);            // 添加/更新
map.get(k);               // 获取（可能 null）
map.getOrDefault(k, def); // 安全获取
map.containsKey(k);       // 键存在？
map.keySet();             // 所有键
map.entrySet();           // 键值对
map.remove(k);            // 删除
for (Map.Entry<K, V> e : map.entrySet()) {}

// HashSet
Set<Integer> set = new HashSet<>();
set.add(x);               // 添加
set.contains(x);          // 包含？
set.remove(x);            // 删除
for (int x : set) {}      // 遍历

// PriorityQueue
PriorityQueue<Integer> pq = new PriorityQueue<>();      // 最小堆
PriorityQueue<Integer> pq = new PriorityQueue<>((a,b)->b-a);  // 最大堆
pq.offer(x);              // 添加
pq.poll();                // 取出（并删除）
pq.peek();                // 查看（不删除）
pq.isEmpty();             // 是否为空

// String
String s = "hello";
s.length();               // 长度
s.charAt(i);              // 第 i 个字符
s.substring(a, b);        // [a, b)
s.indexOf(sub);           // 查找(-1 表示不存在)
s.equals(t);              // 比较(不能用 ==)
s.split(sep);             // 拆分
s.toCharArray();          // 转字符数组
new String(charArr);      // 转回字符串

// Array 工具
Arrays.sort(arr);         // 排序
Arrays.fill(arr, x);      // 填充
Arrays.copyOf(arr, n);    // 复制
Arrays.copyOfRange(arr, from, to);
Arrays.binarySearch(arr, x);
Arrays.asList(arr);       // 转 List
Arrays.toString(arr);     // 转字符串

// Collections 工具
Collections.sort(list);
Collections.sort(list, (a,b)->...);
Collections.reverse(list);
Collections.binarySearch(list, x);
Collections.max(list);
Collections.min(list);
```

### 常用但非必背（出现频率 50-90%）

```java
// Deque（推荐代替 Stack 和 Queue）
Deque<Integer> dq = new ArrayDeque<>();
dq.push(x);       // 栈操作：入栈
dq.pop();         // 栈操作：出栈
dq.offer(x);      // 队列操作：入队
dq.poll();        // 队列操作：出队
dq.peek();        // 查看队首/栈顶
dq.isEmpty();

// TreeMap（排序 Map）
Map<K, V> map = new TreeMap<>();
map.ceilingKey(k);     // 大于等于 k 的最小键
map.floorKey(k);       // 小于等于 k 的最大键
map.subMap(k1, k2);    // [k1, k2)

// TreeSet（排序 Set）
Set<Integer> set = new TreeSet<>();
set.ceiling(x);        // 大于等于 x 的最小值
set.floor(x);          // 小于等于 x 的最大值
set.headSet(x);        // 所有 < x 的值
set.tailSet(x);        // 所有 >= x 的值
```

---

## 🚨 最常犯的 5 个错误

### 错误1️⃣：`list[i] = x` ← ❌ 会编译错误

```java
// ❌ 错
List<Integer> list = new ArrayList<>();
list[0] = 10;

// ✅ 对
list.set(0, 10);
```

### 错误2️⃣：`map["key"]++` ← ❌ NPE

```java
// ❌ 错（get 返回 null）
map.get("key")++;

// ✅ 对
map.put("key", map.getOrDefault("key", 0) + 1);
```

### 错误3️⃣：`String s == t` ← ❌ 比较引用

```java
// ❌ 错
if (s == "hello") { }

// ✅ 对
if (s.equals("hello")) { }
```

### 错误4️⃣：遍历删除 ← ❌ ConcurrentModificationException

```java
// ❌ 错
for (int x : list) {
    if (x > 5) list.remove(x);
}

// ✅ 对1：迭代器
Iterator<Integer> it = list.iterator();
while (it.hasNext()) {
    if (it.next() > 5) it.remove();
}

// ✅ 对2：倒序
for (int i = list.size() - 1; i >= 0; i--) {
    if (list.get(i) > 5) list.remove(i);
}
```

### 错误5️⃣：PriorityQueue 默认最小堆 ← ❌ Top K 错误

```java
// ❌ 错（存的是最小值）
PriorityQueue<Integer> pq = new PriorityQueue<>();

// ✅ 对（存的是最大值）
PriorityQueue<Integer> pq = new PriorityQueue<>((a,b)->b-a);
```

---

## 📋 高频题型模板快速参考

### 模板1：哈希表 + 频率统计

```java
// 题型例子：LeetCode 242 (有效的字谜)
Map<Character, Integer> freq = new HashMap<>();
for (char c : s.toCharArray()) {
    freq.put(c, freq.getOrDefault(c, 0) + 1);
}
```

### 模板2：Top K

```java
// 题型例子：LeetCode 347 (前 K 个高频元素)
PriorityQueue<Integer> minHeap = new PriorityQueue<>(
    (a, b) -> freq.get(a) - freq.get(b)  // 按频率升序
);
for (int num : freq.keySet()) {
    minHeap.offer(num);
    if (minHeap.size() > k) minHeap.poll();
}
```

### 模板3：两指针

```java
// 题型例子：LeetCode 167 (两数之和 II)
int left = 0, right = nums.length - 1;
while (left < right) {
    int sum = nums[left] + nums[right];
    if (sum == target) return new int[]{left + 1, right + 1};
    else if (sum < target) left++;
    else right--;
}
```

### 模板4：滑动窗口

```java
// 题型例子：LeetCode 3 (无重复字符的最长子串)
Map<Character, Integer> window = new HashMap<>();
int maxLen = 0;
int left = 0;
for (int right = 0; right < s.length(); right++) {
    char c = s.charAt(right);
    window.put(c, window.getOrDefault(c, 0) + 1);
    
    while (window.get(c) > 1) {      // 有重复
        char leftChar = s.charAt(left);
        window.put(leftChar, window.get(leftChar) - 1);
        left++;
    }
    maxLen = Math.max(maxLen, right - left + 1);
}
return maxLen;
```

### 模板5：BFS (广度优先搜索)

```java
// 题型例子：LeetCode 127 (单词接龙)
Queue<String> q = new LinkedList<>();
Set<String> visited = new HashSet<>();
q.offer(start);
visited.add(start);
int steps = 0;

while (!q.isEmpty()) {
    int size = q.size();
    steps++;
    
    for (int i = 0; i < size; i++) {
        String word = q.poll();
        if (word.equals(end)) return steps;
        
        for (String neighbor : getNeighbors(word, wordList)) {
            if (!visited.contains(neighbor)) {
                visited.add(neighbor);
                q.offer(neighbor);
            }
        }
    }
}
return -1;
```

### 模板6：DFS (深度优先搜索)

```java
// 题型例子：LeetCode 200 (岛屿数量)
void dfs(char[][] grid, int i, int j) {
    if (i < 0 || i >= grid.length || j < 0 || j >= grid[0].length) {
        return;
    }
    if (grid[i][j] != '1') return;
    
    grid[i][j] = '0';  // 标记访问
    dfs(grid, i + 1, j);
    dfs(grid, i - 1, j);
    dfs(grid, i, j + 1);
    dfs(grid, i, j - 1);
}
```

### 模板7：排序 + Lambda

```java
// 题型例子：LeetCode 56 (合并区间)
class Interval {
    int start, end;
}
List<Interval> intervals = new ArrayList<>();
// 按 start 升序，相同则按 end 升序
Collections.sort(intervals, (a, b) -> {
    if (a.start != b.start) return a.start - b.start;
    return a.end - b.end;
});
```

---

## 💡 JVM 与多线程速知（面试加分）

### JVM 基础（理解即可，不需死记）

```
堆（Heap）
├── 所有对象和数组存放处
├── GC 发生的地方
└── 所有线程共享

栈（Stack）
├── 每个线程一份
├── 局部变量
├── 方法调用记录
└── 以帧形式管理

方法区
├── 类定义
├── 常量池
└── 静态变量
```

### 垃圾回收（GC）

```java
// GC 何时触发？
- 堆内存满了
- System.gc() 被调用（不推荐）
- JVM 自动触发

// 什么会被回收？
- 没有任何引用指向的对象
- 即使还在静态集合中，也是内存泄漏
```

### 多线程基础

```java
// 创建线程
Thread t = new Thread(() -> {
    System.out.println("线程执行");
});
t.start();  // ⭐ 是 start，不是 run

// volatile（保证可见性，不保证原子性）
private volatile boolean flag = false;

// synchronized（保证原子性和可见性）
synchronized void increment() {
    count++;
}

// AtomicInteger（原子操作，比 synchronized 快）
AtomicInteger count = new AtomicInteger(0);
count.incrementAndGet();
```

---

## 🧪 模拟面试题目清单

### 第 1 场：基础题
1. **Simple**: LeetCode 1 (两数之和) - HashMap
2. **Simple**: LeetCode 242 (有效的字谜) - HashMap
3. **Medium**: LeetCode 347 (前 K 个高频元素) - PriorityQueue

### 第 2 场：进阶题
1. **Simple**: LeetCode 20 (有效的括号) - Stack/Deque
2. **Medium**: LeetCode 3 (无重复字符最长子串) - 滑动窗口
3. **Medium**: LeetCode 56 (合并区间) - 排序

### 第 3 场：综合题
1. **Simple**: LeetCode 88 (合并两个有序数组) - 双指针
2. **Medium**: LeetCode 200 (岛屿数量) - DFS
3. **Medium**: LeetCode 127 (单词接龙) - BFS

---

## ✅ 面试前最后检查清单

### 代码习惯（1-2 周养成）
- [ ] 条件判断用 `Integer.MAX_VALUE` 和 `Integer.MIN_VALUE`
- [ ] 避免使用 `==` 比较非基本类型
- [ ] 始终用 `getOrDefault()` 处理 HashMap 查询
- [ ] 优先用 `for` 循环而不是函数式编程
- [ ] 注释每个关键变量的含义

### API 记忆（必须烂熟）
- [ ] ArrayList：add, get, set, remove, size
- [ ] HashMap：put, get, getOrDefault, containsKey, entrySet
- [ ] String：length(), charAt(), substring(), indexOf(), equals()
- [ ] Arrays：sort(), fill(), copyOf()
- [ ] Collections：sort()

### 算法技巧（能快速实现）
- [ ] 两指针可以在 2 分钟内完成
- [ ] 哈希表频率统计在 3 分钟内完成
- [ ] BFS 模板在 5 分钟内完成
- [ ] DFS 模板在 3 分钟内完成

### 边界处理（最易出错）
- [ ] 数组、字符串从 0 开始
- [ ] 循环条件：`i < n`（不是 `i <= n`）
- [ ] substring(a, b)：b 不包括在内 [a, b)
- [ ] 空集合、空字符串的处理

---

## 📚 推荐刷题顺序

### 第 1 周：奠定基础（30 题）
```
Easy HashMap:    1, 383, 202, 205, 242, 290
Easy Array:      26, 27, 28, 35, 53, 88
Easy String:     67, 125, 14, 151
Easy Others:     20, 155, 160
```

### 第 2 周：掌握进阶（20 题）
```
Medium HashMap:  1, 156, 347, 387, 451, 454
Medium Array:    11, 15, 16, 18, 31, 48
Medium String:   3, 6, 5, 438, 567
```

### 第 3 周：理论补充 + 模拟（15 题）
```
Medium Graph:    200, 127, 39, 46, 47
Hard Stack:      42, 84, 85, 321
Mock Interview:  5 道混合题，计时完成
```

---

## 🎓 学习资源推荐

| 资源 | 用途 | 推荐度 |
|------|------|-------|
| LeetCode 官网 | 刷题 + 学习讨论 | ⭐⭐⭐⭐⭐ |
| Java 官方文档 | API 查询 | ⭐⭐⭐⭐ |
| LeetCode 讨论区 | 学习最优解 | ⭐⭐⭐⭐ |
| 本速成指南 | 快速入门 | ⭐⭐⭐⭐⭐ |

---

## 总结：最少准备方案（3天速成）

> 如果只有 3 天准备：

**Day 1**
1. 背 API 速查表（第 1 遍）
2. 代码实战：完成模板1-3（2小时）
3. 相关 LeetCode 题 5 道

**Day 2**
1. 背 API 速查表（第 2 遍）+ 常见陷阱（第 1 遍）
2. 代码实战：完成模板4-6（2小时）
3. 相关 LeetCode 题 10 道

**Day 3**
1. 背 API + 陷阱（第 2-3 遍）
2. 模拟面试 1 场（3 题，30 分钟完成）
3. 查漏补缺 + 复习

**预期结果**：能写出能跑的代码，API 不太熟但能查文档

---

## 最后提示

👉 **最重要的是写代码！** 不要死背 API 名字，通过实做 10-20 题，API 自然就会了。

👉 **对标 C++ 思考**：看到一个 Java API，先想"C++ 中怎么实现"，会加深理解。

👉 **多写注释**：在面试中，逐行讲解代码意思，显得更专业。

👉 **时间管理**：Easy 题 < 5 min，Medium 题 < 15 min，超过就跳过下一道。

---

**祝你面试顺利！** 🚀

